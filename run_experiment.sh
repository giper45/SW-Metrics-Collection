#!/bin/bash
set -euo pipefail

RUN_ID=$(python3 -c 'import uuid; print(uuid.uuid4())')
METRIC_RESOURCE_TRACKING="${METRIC_RESOURCE_TRACKING:-1}"
METRIC_RESOURCE_SAMPLE_SEC="${METRIC_RESOURCE_SAMPLE_SEC:-0.5}"
METRIC_RESOURCE_REPORT="${METRIC_RESOURCE_REPORT:-analysis_out/metric-runtime-${RUN_ID}.jsonl}"
JAVA_BUILD_BYTECODE="${JAVA_BUILD_BYTECODE:-1}"
JAVA_BUILD_STRICT="${JAVA_BUILD_STRICT:-0}"
JAVA_BUILD_FORCE="${JAVA_BUILD_FORCE:-0}"

METRIC_RUN_ID="$RUN_ID" \
METRIC_RESOURCE_TRACKING="$METRIC_RESOURCE_TRACKING" \
METRIC_RESOURCE_SAMPLE_SEC="$METRIC_RESOURCE_SAMPLE_SEC" \
METRIC_RESOURCE_REPORT="$METRIC_RESOURCE_REPORT" \
JAVA_BUILD_BYTECODE="$JAVA_BUILD_BYTECODE" \
JAVA_BUILD_STRICT="$JAVA_BUILD_STRICT" \
JAVA_BUILD_FORCE="$JAVA_BUILD_FORCE" \
make case-study
